import 'package:flutter/material.dart';

/// A utility class that defines theme-related constants for the application.
class ThemeConstants {
  /// The primary color used in the application's theme.
  static const Color primaryColor = Colors.deepOrange;

  /// The secondary color used in the application's theme.
  static final Color secondaryColor = Colors.deepOrange[100] ?? Colors.deepOrange;
}
